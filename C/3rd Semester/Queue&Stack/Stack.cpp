#include"stack.h"
#include<cmath>

//template<class T>
//T& stack<T>::operator[] (size_t id) {
//        if(!last) throw EMPTY;
//        size_t j = QuantifyStack();
//        if(id >= j) throw OUT_OF_RANGE;
//        size_t idNew = j - id - 1;
//        node<T> *temp = last;
//        for(; idNew; idNew--)
//        {
//            temp = temp->next;
//        }
//        return temp->data;
//}
